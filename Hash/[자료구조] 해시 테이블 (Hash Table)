[자료구조] 해시 테이블 (Hash Table)
 
 == 해시, 맵, 해시 맵, 딕셔너리, 연관 배열 모두 같은 말
  : 쌍으로 이뤄진 값들의 리스트
  : 첫 번째 항목 - 키
  : 두 번째 항목 - 값
  : 키 - 값 관계
  : 값 룩업은 한 단계만 걸리므로 평균적으로 효율성 O(1)

- 해싱
  : 문자를 가져와 숫자로 변환하는 과정
  : 글자 -> 특정 숫자로 변환하는데 사용한 코드 = 해시 함수
  : 주어진 문자에 대해 반환하는 결과가 일관되야함 = 해시 함수가 유효

- 충돌 해결
  : 해싱한 키가 동일하면 충돌 발생
  : 고전적 해결 방법 - 분리 연결법 : 해당 인덱스에 배열로 값 넣음

- 훌륭한 충돌 조정
  : 효율성을 결정하는 세 요인
     1. 해시 테이블에 얼마나 많은 데이터를 저장하는가
     2. 해시 테이블에서 얼마나 많은 셀을 쓸 수 있는가
     3. 어떤 해시 함수를 사용 하는가

  --> 사용 가능한 모든 셀에 데이터를 분산시켜야 좋은 해시 함수

  
